seed: 42
classes: ["unripe", "ripe", "overripe"]
img_size: 224
batch_size: 32
num_workers: 0
epochs_frozen: 5
epochs_unfrozen: 10
lr: 0.0003
weight_decay: 0.0001
label_smoothing: 0.05
use_class_weights: true
train_dir: "data/train"
val_dir:   "data/val"
test_dir:  "data/test"
checkpoint_dir: "checkpoints/mobilenet_v3_large"
best_metric: "f1_macro"

fruits: ["apple","banana","orange","pineapple"]
ripeness: ["unripe","ripe","overripe"]

csv:
  train: "data_mt_train/train.csv"
  val:   "data_mt_train/val.csv"
  test:  "data_mt_test/test.csv"

