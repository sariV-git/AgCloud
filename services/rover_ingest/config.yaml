postgres:
  dsn: "dbname=missions_db user=missions_user host=db password=Missions!ChangeMe123"
minio:
  endpoint: "minio:9000"
  bucket: "rover-images"
  access_key: "minioadmin"
  secret_key: "minioadmin"
metrics:
  port: 9109
validation:
  timestamp_tolerance_sec: 300
  future_drift_sec: 5
