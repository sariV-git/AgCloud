pandas>=2.2
confluent-kafka>=2.4
paho-mqtt>=1.6

# Optional: Parquet support (only needed if you want to load Parquet files).
# The simulator works with CSV files without these.
# If you need Parquet, install EITHER pyarrow OR fastparquet (not both).

# pyarrow>=14
# fastparquet>=2024.2
