# Example environment configuration for Storage / MinIO

# Root MinIO service (local)
MINIO_ENDPOINT=localhost:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Hot / Cold MinIO endpoints
HOT_ENDPOINT=http://minio-hot:9000
COLD_ENDPOINT=http://minio-cold:9000

# Hot / Cold bind addresses
HOT_ADDRESS=:9000
HOT_CONSOLE=:9001
COLD_ADDRESS=:9100
COLD_CONSOLE=:9101

# mc aliases
MC_ALIAS_HOT=hot
MC_ALIAS_COLD=cold

# Buckets
BUCKET_IMAGERY=imagery
BUCKET_TELEMETRY=telemetry
