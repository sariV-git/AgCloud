# GUI libs are only installed on Windows dev machines
PyQt6==6.9.1 ; platform_system == "Windows" or platform_system == "Darwin"
PyQt6-WebEngine==6.9.0 ; platform_system == "Windows" or platform_system == "Darwin"
# Web/API
fastapi>=0.110
uvicorn[standard]>=0.29
flask

# Metrics & HTTP
prometheus-client>=0.20
requests>=2.31
httpx==0.27.0   # only needed when you switch to real Flink REST

# gRPC & Protobuf
grpcio>=1.56,<2
grpcio-tools>=1.56,<2
protobuf>=5.26,<6

# Validation / crypto
pydantic==2.7.4
argon2-cffi
